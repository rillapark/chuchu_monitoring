import { formatKrw, formatUsd } from '@/lib/format';
export default function OrderBook({ob}:{ob:any}){return <div className='grid grid-cols-2 gap-2 text-xs'><div><p className='text-rose-400'>ASK</p>{ob.asks?.map((x:any,i:number)=><div key={i} className='flex justify-between'><span>{formatUsd(x.pxUsd)} / {formatKrw(x.pxKrw)}</span><span>{x.sz.toFixed(2)}</span></div>)}</div><div><p className='text-emerald-300'>BID</p>{ob.bids?.map((x:any,i:number)=><div key={i} className='flex justify-between'><span>{formatUsd(x.pxUsd)} / {formatKrw(x.pxKrw)}</span><span>{x.sz.toFixed(2)}</span></div>)}</div></div>}
