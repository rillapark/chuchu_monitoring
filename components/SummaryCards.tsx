export default function SummaryCards({assets}:{assets:any[]}){ const max=[...assets].sort((a,b)=>Math.abs(b.gap.rawGapPct||0)-Math.abs(a.gap.rawGapPct||0))[0]; const over=assets.filter(a=>Math.abs(a.gap.rawGapPct||0)>=1).length; return <section className='grid md:grid-cols-3 gap-3 mb-4'><div className='card'>Max GAP: {max?.nameKo||'-'} {max?.gap?.rawGapPct?.toFixed?.(2)}%</div><div className='card'>|GAP| ≥ 1%: {over}개</div><div className='card'>Funding APR 최고: {[...assets].sort((a,b)=>(b.hyperliquid.fundingAprPct||0)-(a.hyperliquid.fundingAprPct||0))[0]?.nameKo||'-'}</div></section> }
